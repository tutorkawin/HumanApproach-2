consequtiveLetterWordProgram:

inp: a="leoow ready to releasee" 16
o/p: leoo,releasee

leoo
ready
to
release

flag=0;
temp=0;
for(int i=0;i<a.length();i++)
{
			0 != 15
	if(a[i]!=' ' && i!=a.length()-1)
	{
		if(a[i]==a[i+1])
		{
			flag=1;
		}
		
	}
	else
	{
		if(flag==1)
		{ 
			for(int j=temp;j<i;j++)
			{
				print(a[j]);
			}
		}
		temp=i+1;
		flag=0;
		print("\n");
	}
}

